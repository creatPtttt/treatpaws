/** External URLs used by marketing CTAs. Keep these in one place so they stay in sync. */

/** Pump.fun token page (placeholder until the official $TREAT launch URL is live). */
export const PUMP_FUN_URL = 'https://pump.fun';
